# WordPress Database Configuration
# IMPORTANT: Copy this file to .env and update with your values
DB_NAME=wordpress
DB_USER=wordpress
DB_PASSWORD=wordpress
DB_ROOT_PASSWORD=rootpassword
TABLE_PREFIX=wp_

# WordPress Debug Mode (1 for true, 0 for false)
WP_DEBUG=1

# Xdebug Configuration (develop,debug,coverage,profile)
XDEBUG_MODE=develop,debug

# Port Configuration
# NOTE: If running multiple sites, change these ports to avoid conflicts!
# Site 1: 8080, 8081, 8025, 3306
# Site 2: 8082, 8083, 8026, 3307
# Site 3: 8084, 8085, 8027, 3308
NGINX_PORT=8080
PMA_PORT=8081
DB_PORT=3306
MAILHOG_WEB_PORT=8025
MAILHOG_SMTP_PORT=1025
